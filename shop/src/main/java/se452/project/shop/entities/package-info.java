/**
 * @author Feng Wang
 *         CustomerAccount variables for security
 * 
 * @author Alex Sobieraj
 * 
 *         Created the original CustomerAccount and CustomerItem objects. Feng
 *         and I turned these
 *         into Mango objects to work with our MongoDb database.
 * 
 *         Learned that a variable annotated with a OneToOne mapping for an SQL
 *         db
 *         gets annotated with DBRef instead
 * 
 */

package se452.project.shop.entities;
